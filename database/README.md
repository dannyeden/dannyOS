# Logical Data Architecture

This directory holds implementation-neutral logical data models, temporal design
notes, and domain ownership maps. It does not contain production migrations,
database credentials, generated ORM code, or deployment configuration.

PostgreSQL is the preferred initial implementation technology, but canonical
meaning must not depend on a PostgreSQL-specific storage trick unless an accepted
RFC makes that constraint explicit.

