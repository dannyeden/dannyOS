# Schemas

This directory contains implementation-neutral, versioned contract schemas for
canonical commands, events, queries, and configuration definitions.

Schemas are added only after their semantics and owning domain are defined in the
Canon. Published schemas are immutable; compatible evolution creates a new schema
version under an explicit compatibility policy.

